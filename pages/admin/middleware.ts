export default defineNuxtRouteMiddleware(() => {
  // This file ensures the auth-admin middleware is applied to all pages in the admin directory
  // Nuxt automatically applies middleware with matching directory names
});
